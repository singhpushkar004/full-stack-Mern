## Components
    Components are the reusable modules for react .
    types of Components--
    1. Class Component
    2. Functional Component 

## Props => Properties
    properties are the data that can you pass in the components that data can we used by the child components . 
    props are read only .
## Routes
## List
## Hooks
    Hooks are the predefined functions that is provides the special feature .
EX - useState(),useEffect(),useContext() etc .
Rules for using hooks--
   1. kishi bhi condition ke inside hook ko use nhi kar sakte .
   2. Top par declare karna hoga hooks ko.
   3. Hooks are started from use keyword .

1. Phase create the context
2. provide context
3. use the context

# Context Create karna => Context Provide karna => Context Consume Karna .